{{define "shelter-content-header"}}
    <header>
        <div class="pure-g">
            {{with .Shelter}}
                <div class="pure-u-2-3">
                    {{with .PBShelter}}
                        <h1><a href="{{.URL}}" target="_blank">{{.Name}}</a></h1>
                    {{end}}
                    {{with .PBUpdate}}
                        <p>Updated at {{.Created | dateFormat}}</p>
                    {{end}}
                </div>
                <div class="pure-u-1-3">
                    {{with .PBShelter}}
                        <a class="pure-button" href="/{{.Id}}?type=cat">Cats</a>
                        <a class="pure-button" href="/{{.Id}}?type=dog">Dogs</a>
                        <a class="pure-button" href="/{{.Id}}">All</a>
                    {{end}}
                </div>
            {{end}}
        </div>
    </header>
{{end}}
