{{define "animal-content-header"}}
    <header>
        <div class="pure-g">
            {{with .Animal}}
                <div class="pure-u-2-3">
                    <h1><a href="{{.URL}}" target="_blank">{{.Name}}</a></h1>
                </div>
                <div class="pure-u-1-3">
                </div>
            {{end}}
        </div>
    </header>
{{end}}
